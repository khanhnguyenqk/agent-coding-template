# Task: Implement NVIDIA Evaluation Adaptor

## Objective
Find the entry point of the benchmark and copy it to the `nvidia` submodule.

## Context
- Repository Directory: {{ repo_dir }}

## Required Actions

1. Create Submodule
   - Name: `nvidia`

2. Look For Benchmark Entry Point
   - Find how the benchmark is executed (usually a Python CLI script). The README.md file is a good place to start looking for entry point.

3. Copy Entry Point to Submodule
   - Copy the entry point to the `nvidia` submodule

4. Rename Entry Point
   - Rename the entry point to `nvidia_eval_adaptor.py`
